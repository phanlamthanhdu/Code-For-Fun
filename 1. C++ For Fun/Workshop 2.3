#include <iostream>
using namespace std;

int main(){
    int x, sum{};
    cout << "Input : ";
    cin >> x;

    do{
        if(x!=0){
            sum = sum + x;
        }
        x--;
    }while(x!=0);
    cout << "Output : " << sum << endl;
    return 0;
}
